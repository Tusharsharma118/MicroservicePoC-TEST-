spring:  
  application:
    name: account-service
 
  cloud:
    config:
      fail-fast: true
      name: account-service
      discovery:
        enabled: true
        service-id: config-server
      
  
 